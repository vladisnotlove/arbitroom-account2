---
import classNames from "classnames";
import "./Container.scss";

type Props = {
    className?: string;
}

const { className } = Astro.props;
---

<div class={classNames(className, "container")}>
    <slot />
</div>