---
import classNames from "classnames";
import "./Block.scss";

export interface Props extends Record<string, any> {
	className?: string;
}

const { className, ...props } = Astro.props;
---

<div class={classNames(className, "block__title")} {...props}>
	<slot />
</div>
